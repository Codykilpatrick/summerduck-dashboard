/** @type {import('next').NextConfig} */
const nextConfig = {
  // Configure output for static exports if needed
  // output: 'export',
  
  // If your site is not at the root of the domain, set the basePath
  // basePath: '/your-subdirectory',
  
  // Enable this if needed for GitHub Pages
  // trailingSlash: true,
};

export default nextConfig; 